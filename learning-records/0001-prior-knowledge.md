# Prior Knowledge: Theoretical EF Core Exposure

The user has completed tutorials and read about EF Core concepts but has not built a real project with it. They understand the abstract ideas — DbContext, entities, migrations, LINQ — but haven't applied them end-to-end in a working application.

**Implications**: The first lesson should be a complete, hands-on build from scratch (scaffold → model → context → migration → query). This establishes the concrete experience that future lessons can reference. Avoid re-explaining concepts they've already read about; focus on *doing*.

**Evidence**: User stated they have "some EF Core exposure" but clarified further as "just theory / tutorials" — they've "read about it but haven't built anything real."
